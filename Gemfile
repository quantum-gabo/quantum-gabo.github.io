# frozen_string_literal: true
source "https://rubygems.org"

# Core gem for GitHub Pages compatibility
gem "github-pages", "~> 232", group: :jekyll_plugins

# Optional: If you need specific plugins beyond github-pages defaults,
# list them without strict version constraints unless necessary
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-sitemap"
  gem "jekyll-paginate"
  gem "jekyll-seo-tag"
  gem "jekyll-redirect-from"
end
